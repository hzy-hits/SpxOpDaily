# 7640/7650/7730/7740贷记2.15核查

2026-09-08。用户要求核查为何没有展示2.40以上组合。本次只核查代码、决策记录与原始IBKR报价，不修改Bark或任何投递行为，不构建策略收益标签。

## 可锁定的对应记录

用户未提供原消息时间，不能确认唯一原卡。Operational decisions中找到21条相同行权价、2.15贷记的记录（查询限最近400条、06:00–08:05 UTC）。选取其中07:59:59.063819 UTC（北京时间15:59:59，美东03:59:59）用原始`data/raw/provider=ibkr/date=2026-09-08/hour=07/quotes.jsonl`还原。不是用消息或Bark数据推导行情。

决策为NO_TRADE、GTH观察地图，expiry=20260908，即当日到期，并非此前讨论的周五3DTE。map.short_abs_delta=0.15，实际Put短腿Delta约−0.14496、Call约+0.14075。

现金：卖7650P bid5.30、卖7730C bid2.65、买7640P ask4.00、买7740C ask1.80，合计贷记2.15。四腿BBO源偏斜约6.107秒，单腿年龄在30秒内，显示深度足够一套。2.15/10=21.5%，显示四舍五入为22%。

## 同一时刻、相同10点翼宽的比较

| 行权价顺序：买Put/卖Put/卖Call/买Call | 保守贷记 | 记录的短Put/短Call绝对Delta | ≤20Δ上限 |
|---|---:|---:|---|
| 7640/7650/7730/7740 | 2.15 | 14.50 / 14.08 | 满足 |
| 7645/7655/7730/7740 | 2.35 | 23.04 / 14.08 | Put超限 |
| 7640/7650/7720/7730 | **2.75** | 14.50 / **20.32** | Call超限 |

上述报价来自同一冻结截点，均检查BBO年龄≤30秒、偏斜≤10秒及数量深度，不是mid。原始capture未保留独立Greeks observed时间，因此这里只能称“当时记录的Delta”，不能认证完整生产Greeks新鲜度或真实组合成交。相邻Greeks存在不平滑现象，也不应把这些值包装成无误差的真实风险。

扫描当时已采集book中所有记录Delta5–20%、两侧10点宽且BBO合格组合，最高贷记确为2.15。因此，“没有任何2.4以上报价”错误；“这份已采集book在当前严格20Δ限制内没有2.4以上组合”有证据。不能从有限订阅推断全市场没有其他组合。

另在较早06:59:50 UTC的已封存lake中，同样2.15基准存在，而7650/7660/7730/7740当时可报2.45，记录短腿Delta19.46/20.00。两时点不可混用。证据保留于raw-scan.json，但不拿早时点替代用户对应较晚时点。

## 为什么地图会这样选与显示

代码`iron_condor.py::build_iron_condor_map`枚举20/15/10/5Δ档位；每档独立选不超过目标的最近短腿，固定10点翼，再按结构分加非正向曲面修正排序。并非固定20Δ、也并非穷举所有10点翼组合后按贷记排序。

`_ten_wide_from_short_delta`同时要求Delta不超过该档上限，且距离目标不超过0.05。在对应book里，Put侧最大合格值0.14496、Call侧0.14075，距离0.20分别0.05504及0.05925，均超过允许偏差，所以不能形成20Δ档；15Δ档仍可形成。更近的Call7720记录Delta0.20316，超过严格0.20天花板，并不会按显示取整放行。

`desk_strategy_view.py`观察地图分支却硬编码`20Δ/10宽`，没有使用map.short_abs_delta或实际腿Delta。它让用户误以为这张已是20Δ档。此前助手据此解释为“系统只锁定20Δ”不准确，已纠正。

显示需要区分目标档、实际Put/Call Delta和翼宽；但这个函数参与人读输出，用户要求Bark不动，本次仅标记问题，没有修改共享渲染或投递行为。

## 是否错误发出了交易授权

对应记录明确NO_TRADE；GTH扩张–收缩状态waiting，理由包括波动输入缺失、峰值年龄、扩张/收缩不足、跨式未衰减和15m IV未收缩。

GTH授权贷记底线为翼宽25%，10点翼即2.50。2.15不满足，2.40也不满足。此前提到的23%例外是特定RTH合同，不适用于此GTH记录。2.75虽然通过贷记这一项，但记录Delta超限，且不能凭贷记绕过扩张–收缩等其他门。

本次没有发现这条2.15地图被授予交易权限的证据，也没有读取Bark送达记录。未改变Delta上限、报价门、候选空间或生产服务。

证据目录：`/srv/data/spx-spark/research/ic-credit-audit-2026-09-08`，含对应决策地图、原始capture截点book、替代组合、较早lake扫描及脚本。该研究目录的策略记录仅用于解释用户指定显示，不用于筛选回测样本、生成PnL或证明edge。

复杂度：生产文件新增/删除0/0，净生产LOC 0；依赖、配置、服务/timer、数据库/表均零变更；legacy路径删除0。没有部署或全面核验生产行情/通知状态。
