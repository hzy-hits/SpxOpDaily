# v69：未来60分钟蝶式与5%资金流覆盖

对应架构执行计划S1/S3（原Phase8）。用户明确选择“未来60分钟位置”，要求从11:00开启、覆盖门降至5%，并合并部署。与同批RTH铁鹰取消每日/早盘限制一起发布；不新增策略类型、服务、数据库、依赖或自动下单。

## 蝶式合同

原CLOSE_CONVERGENCE_60M setup改用`physical_rolling_online_pool_60m.v2`、`raw_spx_es_clock_matched_60m.v2`，不混用旧收盘模型证据hash。11:00 ET起冻结最新完整分钟，target_at=as_of+60分钟；目标不能晚于当日收盘，普通日最后预测锚点15:00。独立过去交易日必须具有同一开盘偏移的前缀和完整60分钟后缀，至少15个训练session，45个日历日窗口。当前路径仅使用锚点时已到达的原始券商行情及同样截止的缓存样本。历史在线混合权重也按对应60分钟结果学习；不是用15:00权重预测11:00。

保留历史字段名settlement_quantiles，但新版本的target_kind明确为forward_60m，其含义由target_at决定，不能当作当天结算分布。候选证据要求当前分钟、前一交易日及更早训练截止、60分钟跨度和当前模型/特征版本。

保留10/15/20点Call/Put蝶及现有借记/翼宽45%、定义风险1000美元、exact BBO、shock、宏观等门。按各腿入场IV计算目标时刻剩余期限的价值变化，以入场组合bid锚定，组合价值限制于0到翼宽；减去入场ask及四份合约双边10.56美元费用。此sticky-IV投影不是未来可执行报价或已验证概率。IV不可用时不使用到期收益代替。每个候选的管理hard exit绑定预测target_at，不再固定15:55，也不从延迟入场时间重新顺延60分钟；无小时内premium stop/trail。仍明确前向未验证，排名最高不保证正收益。

## 冷启动与故障边界

原始库首次扫描实测约82秒，后续复用先前session分钟数据约0.6–0.7秒。实时Core调用方以nonblocking方式请求；普通build_strategy_decision默认同步构造模型，确保离线单次回放不会因后台未完成而误记NO_TRADE。实时调用，现有模型owner使用标准库单线程executor及内存缓存；最多一个准备任务。准备中或当前分钟模型不可用时，该蝶式无候选，其他策略继续。旧分钟完成结果不冒充新分钟模型；无新进程、队列服务、持久化结果或第二决策owner。

## 资金流核查

`captured_option_flow.v5`只将可分类成交量/累计成交量增量门从10%改为5%。20分钟连续路径、最近5分钟新高/新低后反向确认、成交指纹去重、5秒成交到达延迟、内部成交unknown、冷却和退出/减仓权限保持。没有把资金流proxy升格为新开仓或真实买卖开平仓数据，Bark传输未改。

原始Schwab固定2026-09-10 10:40–11:10 ET窗口，170,353条记录、164个合约：

| 口径 | 成交量增量 | 可分类last size | 覆盖 |
|---|---:|---:|---:|
| 原始更新序列 | 204,456 | 39,983 | 19.56% |
| 每5秒仅消费各合约最后快照 | 203,376 | 19,696 | 9.68% |

窗口内没有成交量计数下降；把每条快照复制一遍，增量和分类数量不变。分母并非简单重复累计成交量；采样丢失last trade会减少分子、但累计volume仍覆盖被漏掉的成交。这是固定窗口反例与机制核查，不是生产消费者调度的精确重放，也不证明全天分母无误。5%会增加可用观察，不能补回遗漏成交。此前205个窗口最高7.77%的数字来自运行时保留聚合，不能与这个固定原始窗口混为同一样本。

## 原始报价验收及限制

研究工件位于`/srv/data/spx-spark/research/rolling-60m-flow5-2026-09-11/`：model_probe.py/json、flow_probe.py/json、butterfly_quote_probe.py/json。未用策略卡、NO_TRADE或Bark记录筛选或生成收益。

固定9月10日11:00、13:00、15:00三个预测锚点均可从31个先前session构造分布，训练最晚9月9日。每个时点枚举6种结构，按原始入场ask和目标时刻exact combo bid检查，18种中17种有合格退出报价，1种保留缺报价。仅枚举对照，没有把18种结构当18次独立交易：11:00六种结构费用后均亏损（每组-50.56至-20.56美元）；13:00该时点六种为正。15:00还有两种借记比例超过45%，不能计入合格交易。尚未执行完整政策级选择/成交概率研究，不能据此声称新增时段有稳定edge。

历史独立研究脚本`docs/notebooks/spx-one-month-strategy-edge-2026-08-29.py`明确冻结原15:55管理v2，避免导入新版生产常量后悄悄改变历史标签；生产新候选继续使用动态目标v3。

## 验收清单

5%通过、略低于5%拒绝，重复成交去重；11/13/15时钟各自对应12/14/16目标；此前时钟关闭；模型后台准备不阻塞决策；历史与当前前缀对齐；目标时刻退出不随入场延迟顺延；15:55前结束观察不能冒充完整退出；原始BBO跨价、四份费用及未完成样本保留。发布验证结果与实际运行状态以交付说明为准。


## 发布验证

全量Python 3433项通过（两条依赖弃用警告）；实时后台/离线同步调用补修后相关286项通过。Rust fmt、Clippy与全workspace/all-targets/all-features测试通过，Ruff、Import Linter及模块预算通过。两批合计生产文件新增/删除0/0、修改12个、净增加38行；依赖、配置键、服务、timer、数据库、表均无增删。新增一个惰性后台线程，删除RTH旧配额/锁腿/早盘限定及生产固定15:55/到期payoff估值路径；旧15:55仅在原独立历史研究合同内保留。
