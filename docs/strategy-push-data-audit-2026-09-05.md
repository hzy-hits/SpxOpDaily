# 策略推送、真实报价与代码收口验收

基线为 `e6ef9dbd9b28974790033c6b9656ca39f3f18cca`。本轮继承第一轮未提交修复，
继续处理执行方案 **S1/S2/S4/S6、P3-1/P4-1/P4-2** 的既有 Python owner。
没有部署、重启、发送通知或修改行情湖。未增加生产模块、服务、数据库、队列或依赖。

**范围纠正：83 个送达交易事件只覆盖推送审计，不能代表近两个月行情可支持的全部研究。**
本轮全历史补查确认 IBKR 有 45 个 RTH 日期存在新鲜 0DTE 双边报价，Schwab 有 40 个。
固定回踩规则在这 40 个 Schwab session 中重建出 162 个价格触发；这不是 162 笔已授权或
已成交交易。不能依据三个送达回踩样本给所有策略作收益排名。完整覆盖与旧研究遗漏见下节。

**独立复核更新：此前 −$36.86 的均值已撤回。** Decimal 现金账本发现三个止损边界
受浮点误差影响，补回决策时已收到且仍新鲜的起始报价后，又纠正了三个退出时点。
六个退出时点、其中三个金额发生变化。纠正后的“决策时假设入场”均值为 −$36.39，
仍不能代替送达后执行结果；其中三个退出发生在卡片送达前。以下数字以最终复核为准。

## 推送审计使用的数据与分母

从 `/srv/data/spx-spark/spx.sqlite` 开启只读 SQLite 事务，冻结选中决策、通知 envelope
及首次成功投递时间。报价来自 `/srv/data/spx-spark/data/lake/quotes/schema=v1`。
全湖约 8.79 GB；回放只取通知对应结构的逐腿行情，没有将全湖大小当作有效证据量。

| 观察事实 | 数量 |
| --- | ---: |
| 持久化 selected 决策 | 6,612 |
| 本轮冻结的 NO_TRADE 行 | 186,510 |
| selected 的唯一 opportunity | 105 |
| 未关联交易通知的 selected opportunity | 21 |
| strategy_decision 来源的通知渠道记录 | 480 |
| 交易通知：唯一事件 / 渠道记录 | 84 / 252 |
| 交易事件至少一个渠道送达 / 全部失败 | 83 / 1 |
| 观察提醒：唯一事件 / 渠道记录 | 76 / 228 |
| 交易通知涉及的独立 session | 16 |

此前引用的 186,430 是较早快照，Core 仍在追加 NO_TRADE。6,612 是逐周期记录，
不能充当独立交易数量。477 条已送达渠道记录 = 249 条交易渠道记录 + 228 条观察渠道
记录；分别对应 **83 个交易事件、76 个观察事件**。已送达记录都有成功 attempt，
用于关联的 `(opportunity_id, decision_at)` 在本批数据中没有重复键。

交易事件覆盖 2026-08-10 至 2026-09-04。使用 `event_id` 对应的 opportunity 加
`envelope.occurred_at == decision_at` 关联，**84/84 精确匹配**，没有拿同一机会更早或
更晚的候选代替推送时实际选中的结构。渠道副本、未送达事件、未通知的候选均没有冒充成交。

首次成功投递相对决策的延迟：P50 7.94 秒，按排序取样的 P90 32.05 秒，最大 260.31 秒。
**12/83 已送达交易事件在首次成功投递时已经超过原报价有效期**；没有超过观点有效期的事件。
这里使用 notification_attempts 的成功完成时间，未把沿用旧动作时钟的 created_at 当作真实到达时间。

## 按当时管理合同回放

优先使用记录中的完整 management contract。旧记录通过其 `runtime_git_sha` 从 Git
读取 ManagementPolicy 的字面常量，不执行历史代码，不把今天的 15:45 规则套到旧的
20 分钟政策。历史 Git 标识不能证明当时进程没有未提交修改；这是仍然存在的证据限制。

回放保留 source_at 和 received_at。每个组合时点逐腿重新检查报价年龄、源时间偏斜、
非交叉双边报价与数量；迟到的旧 source 不能覆盖更新 source。缺口超过现有 60 秒研究
采样容差时不产生完成标签。退出报价允许等待至合同截止后的首个合格组合，最长 60 秒，
不拿截止前最后一笔价格伪装为定时退出。铁鹰使用回购负债、蝶式按四份合约计费。

| 已送达事件的结果 | 数量 |
| --- | ---: |
| COMPLETE_EXIT | 53 |
| QUOTE_GAP | 19 |
| CENSORED：观察结束但未退出 | 6 |
| SESSION_ERROR | 1 |
| 旧铁鹰无法恢复为可用贷记管理合同 | 4 |
| 合计 | 83 |

另一个未送达事件也能够算出报价退出，但从已送达结果统计中排除。
4 个不可用旧铁鹰均来自 v35 的 GTH 路径，不能换成 v66 的 TP/SL 合同后宣称重现了旧政策。

53 个完整样本每组按项目约定的每份合约每边 $1.32 计费，均未假设实际成交：

| 历史结构与管理合同 | 完整样本 | 平均净模拟损益/组 |
| --- | ---: | ---: |
| Call Butterfly，旧 v1 | 4 | −$69.31 |
| Put Butterfly，旧 v1 | 5 | −$55.56 |
| Put Debit Vertical，旧 v1 | 14 | −$9.21 |
| Call Debit Vertical，旧 v1 | 14 | −$66.35 |
| Put Debit Vertical，v2 | 6 | +$122.22 |
| Call Debit Vertical，v2 | 5 | −$163.28 |
| RTH Iron Condor，TP50/SL200/15:45 | 2 | +$114.44 |
| 收敛 Put Butterfly，15:55 | 3 | −$153.89 |

完整样本合计平均 **−$36.39**，最差 **−$535.28**。再扣每组 0.05/0.10/0.20 点的
总滑点假设时，均值分别为 −$41.39/−$46.39/−$56.39。这不是成交概率或实际滑点估计。

不能将这张表解释为当前策略的期望收益：政策跨版本，缺失结果不是随机缺失，行情采样
可能错过短暂触发，历史入场限价是否成交未知；也没有冻结参数后独立留出的策略验收集。
2 个可完成 RTH 铁鹰和 3 个收敛蝶不足以选“最优” delta、翼宽、中心或止损。

## 收到推送以后：原限价是否还能使用

对同一批 83 个送达事件，冻结 0/15/30/60 秒反应延迟，在送达后检查首个合格 BBO，
最多等待 60 秒且不得越过观点期限。入场按统一研究口径要求逐腿 age ≤15 秒、skew ≤2 秒，
没有把较宽松的持仓管理报价容差拿来当入场许可；该口径不等于恢复了所有历史 GTH 门控。
第一笔新鲜 BBO 不满足卡片原限价就记录不可用，**不向后搜索更便宜的时点**。
满足时才按原限价假设入场，时间止损从这个假设入场时刻计算，继续检查持有期间报价缺口。

| 送达后的反应延迟 | 原限价在首个合格 BBO 不可用 | 入场缺报价 | 完整退出 | 完整样本条件均值/组 |
| --- | ---: | ---: | ---: | ---: |
| 0 秒 | 16 | 3 | 41 | −$34.12 |
| 15 秒 | 24 | 3 | 33 | −$51.42 |
| 30 秒 | 29 | 3 | 28 | −$73.40 |
| 60 秒 | 31 | 3 | 25 | −$67.95 |

每行分母始终为 83；其余为报价缺口、观察截断、会话错误、合同不可恢复，60 秒情景另有
一个机会到期。详细逐事件结果保留全部状态，未完成项不记成零收益或悄悄消失。
各行是同一批交易的相关情景，不能当成新增独立样本，也不能用不同完整样本集合的均值
推断“多等 60 秒更好”。L1 BBO 穿过限价仍不能证明多腿能同时成交、深度足够或用户实际执行。

本批数据不支持保留“推送已经验证可盈利”的结论，也不足以依据盈亏事后删除某类策略。
保留的是经过独立检查的计算和事实；结构级增量、当前策略选优及完整执行收益仍未通过验收。

## 全历史补查：采集量为何没有变成上面的样本量

本节对应 S4/S6 的数据与证据范围核对。只读扫描完整报价湖和既有候选记录，产物写到
`/tmp/spx-full-history-coverage-2026-09-05/`，没有改动生产代码、运行数据或策略阈值。

### 完整行情确实存在

Parquet footer 盘点得到 4,979 个文件、64 个 UTC 日期分区，日期为 2026-07-04 至
2026-09-05。全部 provider 合计 437,896,435 行，其中 Schwab 266,782,960 行、IBKR
167,613,743 行。这里包含重复更新、非 SPXW、冻结行情及其他 provider；行数不是独立证据。

进一步扫描每个日期的 RTH（09:30–16:00 ET），只对同日到期 SPXW 计数：

| 数据源 | 存在新鲜 0DTE 双边报价的 RTH 日期 | 日期范围 | 这些日期的 RTH 0DTE 原始行数 |
| --- | ---: | --- | ---: |
| IBKR | 45 | 07-06 至 09-04 | 30,653,713 |
| Schwab | 40 | 07-13 至 09-04 | 139,818,735 |

两家交易日重叠，不能相加为 85 个独立 session。新鲜双边报价在本盘点中要求 live、
source_at 不晚于 received_at 且年龄不超过 15 秒、非交叉双边价；这是逐行数据盘点，
不是对所有历史策略的入场门控复原，也未证明某一时点的全部组合腿都能同时成交。
至少 95% 的 RTH 分钟中存在任一合格 0DTE 报价的日期：Schwab 30 天、IBKR 8 天。
该分钟指标不作为整日删除门，也不能替代具体持仓期间逐腿检查。

### 限制来自审计入口及策略、研究的时间范围

1. `audit_strategy_pushes()` 从 `trade_ready` 通知反查选中决策。因此未投递候选、
   NO_TRADE 中的结构、策略上线前的原始行情不会进入该回放。这个入口适合审计用户
   收到的卡片，不适合回答全部历史是否存在策略机会。
2. 统一策略决策表始于 08-07，selected 始于 08-10。selected 的 6,612 个逐周期记录
   归并后是 105 个 opportunity，不能代表 07 月的采集范围。另有 11,222 个
   `shadow_candidate` 行，上轮实际推送回放没有把它们当作收益样本。
3. `strategy_v3_freeze_acceptance.py` 明确只处理 08-05 至 08-08；08-05/06 是冻结
   fixture 验收，08-07/08 的 Pass-B 默认最多抽取 40/50 个分钟样本。它不是全历史回测。
4. `market_regime_denoising.py` 的生产触发起始日是 08-20；不能要求 07 月的通知
   记录里存在这个当时尚未启用的 setup，但可以在 07 月原始数据上作明确标记的固定规则回放。
5. 现存去噪研究 snapshot 生成于 08-19，覆盖截至 08-17 的完整 session，研究脚本
   默认 `RESEARCH_END_DATE=2026-08-18`。该旧产物中的前向样本数 0 不是截至 09-04 的数据结论。

### 已执行的固定规则全历史触发回放

复用现有 `_append_sample()` / `_detect()`，按每 5 秒的知识截点读取已经收到的
Schwab SPX 报价，拒绝缺失来源时间、源年龄超过 15 秒以及乱序源时间覆盖。固定每分钟
第 5 秒判断，保留同方向 900 秒冷却，不搜索新参数。

| 观察结果 | 数量 |
| --- | ---: |
| 扫描日期：07-13 至 09-04 | 40 个 session |
| 新鲜 SPX 五秒网格覆盖至少 95% | 38 个 session |
| 价格触发 | 162（UP 78 / DOWN 84） |
| 08-20 以前，以现行固定规则回溯 | 103，28 个 session |
| 08-20 至 09-04，以相同固定规则回放 | 59，12 个 session |
| SQL 中已选中的 PREAVERAGE15_PULLBACK | 7 个逐周期记录、4 个 opportunity、4 个 session |
| 已送达的该 setup | 3 个交易事件 |

这些不是同一入口逐级淘汰的完整漏斗：历史运行的采样时钟、其他门控与本次固定网格
尚未逐项重建。162 是价格触发，尚未全部重建 60Δ/15 点双腿、候选门控、排序与持仓
管理收益，不能把它当作 162 笔可执行或独立盈利交易。08-20 以后的回放也不会因为日期
在上线以后就自动成为未见样本。原始 received/source 时钟已检查，事后写入数据湖的
可用性边界仍需用相应 ingestion lineage 验证。

### 上轮未纳入的既有研究

- 去噪研究的 `preaverage15_pullback` 已记录 83 个触发和 69 个 exact-BBO 回放样本
  （开发 27、回顾验证 33、已见尾段 9）。其退出是标的对称 first-passage，费用每份
  每边 $1.50；不能和当前 premium stop/trail/15:45 管理、每份每边 $1.32 的结果混合。
- 收盘收敛分布研究记录 29 个完整 session、14 个时序评估 session；该数字评价标的
  分布，不是 29 笔蝶式期权净收益。
- 现存铁鹰分腿止损研究文件记录 20 个首候选 session，14 个 strict-ready、1 个
  diagnostic-ready、5 个 unresolved。该产物及其多种参数结果尚未在本轮逐笔重算，
  不能直接继承其中的收益结论，也不能因为没推送就当成没有这份研究。

因此，“仅这几笔推送不足以验证收益”仍然成立；“两个月数据只能提供这几笔研究样本”
不成立。后续三类结构的完整收益验收应从历史行情重建候选，保留报价缺失和门控失败，
再与真实推送作对照。不能先按旧系统是否发送筛选，再用筛选后的结果评价全部策略。

可复核产物：`rth-coverage.csv/json`、`preaverage-trigger-replay.json`、
`persisted-candidate-funnel.json`、`summary.json`；同目录保存执行脚本与校验清单。
本节为覆盖与触发验收，没有声称完成三类结构全历史期权收益重算。

## 已完成的修复

- **选择与锁**：例外只作用于对应候选；selector 和 delivery 共用同一方向锁规则。
  锁外候选不留在可晋升列表。第一名被路径 veto 后继续检查下一名，每轮最多三个；
  预算耗尽明确记录 `path_evaluation_budget_exhausted`，不声称剩余候选都已检查。
  保留全部阶段 gate audit，包括模型、方向锁与路径淘汰理由。
- **最终记录**：报告读取 Core 的最终策略导出，移除报告端再次调用 selector 的路径。
  交易决策保存完整管理合同、有效策略常量、依赖锁文件哈希、事实与内容哈希；
  收益模型附加工件内容哈希。报告和入队拒绝被修改或来自未来的受哈希保护记录。
  NO_TRADE 不表达账户空仓；持仓事实为 UNKNOWN。重复 NO_TRADE 的既有压缩仍保留。
- **动作时钟**：在实际入队前重取时间，过期机会不入队，旧报价明确提示刷新。
  观点 TTL 不再被渲染/研究耗时悄悄延长。队列等待仍可能使报价过期，因此所有卡片
  继续展示报价截止时间并要求提交前刷新，不宣称送达价格可直接成交。
- **日历**：各来源保留独立 last-good、覆盖区间、刷新时间、失败与重试时间。
  部分成功不替换失败来源；未来时间、过期覆盖、空 seed 不能证明“无事件”。
  任一 pre-event 都优先阻断。coverage 只证明既有 CPI/PPI/就业/FOMC allowlist，
  不代表整个宏观事件全集。Core 只读；现有 Huey Worker 每五分钟检查，六小时成功 TTL，
  失败五分钟再试；通知使用现有队列优先级。
- **关键路径与故障**：Greeks/Spring 解释及事后标签移至最终交易卡入队之后，研究失败
  留诊断而不阻断已完成决策或行情帧输出。可选 Globex/alert/Steven 周期任务失败独立记录，
  现有 cadence 再试；sampler、provider control 和必要 owner 的异常仍上抛。
  授权需要的收益模型和路径 veto 继续执行，没有为提速绕过。
- **观测与数值**：ATM 保存实际两腿源时间、provider 和 strike；重复消费同一观测不能
  增加次数，每五秒源时间桶最多计一次；ATM/provider 坐标改变重置局部压缩证据，整段
  高低仍为展示背景。不同 ATM strike 的 15 分钟跨式不直接比较为同一结构衰减。
  铁鹰 3C 超过翼宽时明确展示阈值不可在正常到期价值范围内达到，没有擅改止损比例。
- **公共标签**：补齐两腿、三腿、规范四腿的数量，支持贷记，保留不完整/缺报价/不支持等
  结果及完整分母；研究去重读取指定只读数据库，移除依赖本机通知配置的逐条查询。
  default policy EV 表排除其他管理版本并给出数量，候选拒绝合同不匹配的 EV 展示。
- **独立核验后继续修复**：组合相加的机器舍入不能漏掉等于阈值的止损；现金回放保留负的
  debit liquidation（这代表平仓还需付现金），非有限数字不能成为完成收益。完整冻结合同
  不得被旧 management_plan 的版本字符串覆盖。收敛蝶原来用 1000% 启动阈值近似关闭追踪，
  合成的 0.10 点入场 / 1.20 点升值 / 0.70 点回落会在 15:11 提前退出；现在用 None 明确
  关闭，管理版本为 `management_policy.close_convergence.hold_1555.v2`。旧记录仍按各自 v1
  合同回放。负组合现金成本和便宜蝶提前退出由合成反例确认，未声称本批历史已经因此亏损。

策略版本更新为 **strategy_policy.bootstrap.v66**，未扩大候选种类或自动下单权限。
第一轮损益符号、止损截断、费用、模型因果边界与不可变图片修复继续保留。

## 测试收口与可复现入口

除了第一轮删除的源码形状测试，本轮删除了 tests/conftest.py 的全局
`isolate_strategy_edge_authority`：它原来将所有测试中的收益授权替换为全部通过。
集成测试现在运行真实授权逻辑。旧的“有好看的分布就该出蝶”断言改为：
分布仍可解释候选，但缺少所需模型时不得取得人工授权。测试启动也不再读取 `.env`。

回归覆盖组合单腿冻结/迟到、贷记 3C 实际亏损止损、合约数量、不完整标签、日历部分失败
与事件重叠、重复 ATM 输入、候选级 veto、锁例外、最终记录一致、实际动作时钟和可选任务故障。
Import Linter 与模块体积上限没有放宽。

最终验证：`uv run pytest -q` **3,312 passed**（105.12 秒）；两条提示来自
websockets / Starlette 的依赖弃用警告。`uv run ruff check src tests scripts`、
`uv run lint-imports`（453 文件、1,892 条依赖、两个合同）及 `git diff --check` 均通过。
没有 Rust 改动，本轮未执行 Rust 发布检查。

直接从数据库和报价湖复现（只读源，写独立研究目录）：

```bash
uv run python -m spx_spark.data_platform.research.strategy_decision_replay \
  --database /srv/data/spx-spark/spx.sqlite \
  --data-root /srv/data/spx-spark/data \
  --output-root /tmp/spx-push-audit-new
```

本次重新冻结选中决策、通知和约 119 MB 实际使用的逐腿报价。缓存复用覆盖窗口，
送达后的 20 分钟退出仍有相应后续行情，不用早期窗口强行截断。最终结果目录为
`/tmp/spx-push-audit-2026-09-05-skeptical-verified`，源快照为
`/tmp/spx-push-audit-2026-09-05-skeptical`；冻结模式核对输入哈希，不回退到变化中的湖，
输出目录必须为空，避免覆盖旧研究证据：

```bash
uv run python -m spx_spark.data_platform.research.strategy_decision_replay \
  --snapshot-root /tmp/spx-push-audit-2026-09-05-skeptical \
  --output-root /tmp/spx-push-audit-recheck
```

独立核验脚本 `independent-cash-verifier.py` 与结果 `independent-cash-check.json` 存于
最终研究目录。它不用项目的组合报价构造器或策略模拟器，使用 Decimal、按接收时间的
二分查找、逐腿现金收支和独立退出判断；仅复用历史合同的字面常量读取。
**80 条合同可恢复记录、79 个已送达记录 ×4 =316 个反应场景，退出时间、状态和净现金
结果无差异**。四个旧铁鹰合同不可恢复，未冒充通过。原始计数再与只读 SQL 独立核对。
这是计算交叉验收；相同程序的重复结果只证明可重复，不能单独证明经济含义正确。

这些历史数据验证了推送关联、费用、管理、时间与缺失结果处理，尚不能完整重建旧版本的
候选枚举及每个外部输入当时入湖的事实。没有据此训练新模型、搜索参数或宣称特征消融通过。
完整发布清单、独立样本概率校准、成交/未成交证据及策略增量仍需分别验收。

本机只读检查时 Core、Worker、IBKR stream、Schwab marketdata/OAuth 为 active/running，
Core/Worker NRestarts=0；没有验证新代码已被进程加载，也没有用进程状态证明行情新鲜。
未执行部署脚本、commit、push、服务重启或真实通知测试。

## 复杂度账本与发布影响

以下相对固定基线累计计算，包含第一轮修复；`src/` 也包含离线研究代码。

| 项目 | 新增 | 删除 | 净变化 |
| --- | ---: | ---: | ---: |
| 生产文件 | 0 | 1 | −1 |
| 生产代码行 | 1,208 | 788 | +420 |
| 依赖 | 0 | 0 | 0 |
| 配置键 | 0 | 0 | 0 |
| service / systemd timer | 0 | 0 | 0 |
| 数据库 / 表 | 0 | 0 | 0 |

本轮生产文件新增/删除为 0/0，净增加 113 行；上表为三轮累计。代码增量主要来自
既有研究入口中的真实推送回放、报价快照和决策/覆盖证据。
复用现有 Worker 增加一个日历周期任务，没有新建队列或进程。移除的旧路径包括
LatestMarketProjectionStore 空壳与 latest_projection 转发模块、报告端再次选优、
Core 同步联网刷新、重复收益统计函数、全局授权 passthrough 和源码形状断言。
本轮进一步删除负现金平仓截零、1000% 假关闭追踪和只按决策时钟评价推送执行的路径。

旧路径方法及 entry_edge.v1 模型不能继续作为新政策的有效收益证据；旧模型通道需要
按完整管理合同重新训练、验收。本轮没有替换运行时模型或自动扩大人工例外权限。
宏观覆盖规则生效前也需由 Worker 取得来源覆盖；未知覆盖不能沿用旧 seed 授权。
