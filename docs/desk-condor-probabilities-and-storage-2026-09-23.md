# 铁鹰概率摘要与冗余研究副本回收

范围：S1/S3 的既有 Desk Map 展示与路径统计；Phase 7 延期期间的真实磁盘故障处理。
复用现有渲染、模拟器和去重 manifest，不新增服务、依赖、数据库、配置或 wire contract。

## 用户可见行为

铁鹰摘要展示三项不同的历史路径模拟频率：

- 费用后盈利：已完成管理路径的净损益严格大于零的比例，零收益不算盈利。
- 止盈：按当前铁鹰管理规则先达到止盈条件的比例。
- 止损：模拟实际以 credit stop 退出的比例，不以 premium stop 字段代替。

正文同时显示管理退出时刻、独立历史交易日数及“未校准”。这不是期权 Delta、
到期留在短腿之间的概率、成交概率或已证明的实际胜率。模拟仍依赖历史路径和估值假设，
本次没有重新校准模型，也没有证明新的策略 edge。

RTH 的最迟退出是 15:45 ET，GTH 为对应清算日 12:30 ET。未知/不匹配合同、
未就绪路径、不足样本或缺失/非法统计显示“概率暂不可估”。已选候选不得借用
另一个地图扫描结构的概率。原交易门槛、候选权限、原始诊断数据和管理规则保留。

原 Delta/EM/位移/摩擦/扫描数量及 P10/P50/P90 不再堆入铁鹰正文；
D3/D4/SNR 研究曲面说明退出 Desk View，仍可在决策数据中审计。
完整交易腿与价格继续由现有人工作业卡提供；Bark 代码、配置和投递未修改。

## 数据清理合同

目标为 `/srv/data/spx-spark/research/raw-deduplicated-2026-09-05/lake` 这份
额外的研究派生集。原始 1,252 个 Parquet 分区约 7.67 GB，派生集约 9.21 GB；
派生格式保存每次接收记录，使减少状态行数并未减少文件总大小。

清理先逐分区验证原始与派生文件 SHA-256、大小、manifest 完整性及先前的无损验证结果，
确认没有打开派生文件的本机进程；全部通过后才删除 manifest 列出的派生文件。
原始券商 Parquet、去重合同、manifest 和仓库内重建脚本保留。
2026-09-23 00:27:28 UTC 全部校验通过，已删除 1,252 个派生 Parquet，
回收 9,211,777,996 字节（8.58 GiB）；原始 1,252 个分区保留。
实际完成状态及释放字节数记录在原研究目录的 `redundancy-cleanup-audit.json`。
这不是对原始行情做破坏性去重，也不改变生产 data_root。

需要重建时，使用原研究说明的 `--deduplicate-only` 命令和新的空输出目录重新生成。
本目录保留的旧 manifest 用于审计，不能以 `--resume` 将它当作派生文件仍存在的证明。
旧原始行情的因果时间线保持不变。

## 验证与复杂度

验收覆盖 GTH/RTH 管理合同、候选和扫描结构隔离、缺失/非法概率、费用后零收益、
上涨亏损路径与止损频率、渲染不修改决策数据。全量 Python 3,497 项通过；最终相关检查 187 项通过。Ruff、Import Linter、
Rust fmt/Clippy/workspace tests、`git diff --check` 通过；两条既有 Python 依赖弃用警告保留。
生产文件新增/删除 0，修改现有五个文件，净减 159 行；依赖、配置键、服务/timer、数据库/表增删均为 0。
删除铁鹰重复的原始指标渲染及 Desk View 曲面附加路径；不新增兼容包装层。
